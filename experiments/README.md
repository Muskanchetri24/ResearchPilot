# Experiments Directory 🔬

This directory tracks structured experiments for:
- RAG evaluation benchmarks (Faithfulness, Context Precision, Hallucination metrics).
- Vector DB performance comparisons (Chroma vs. Qdrant vs. FAISS).
- Reranking model evaluations (Cohere Rerank, BGE-Reranker).
